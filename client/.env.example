# Local: copy to .env.local (gitignored)
# Vercel: add the same VITE_* keys in Project → Environment Variables

# API — use /api in dev (Vite proxies to port 3001), or your hosted API URL in production
VITE_API_URL=/api

# Public site URL (optional)
VITE_SITE_URL=http://localhost:3000
